Description

This C language based library helps us to perform all crud operations with linked list based datastructure.

Operations:
Add new node in the beginning,
Add new node in the tail position,
Print the nodes,
Delete the node at the beginning,
Delete the node at the tail,
Count the nodes,
Delete the matching nodes.
